# Contributing

Open substantial changes from `dev`, add tests for behavioral changes, and run `make -C sw test` plus HDL tests when tools are available. Never commit generated vendor output, course collateral with unknown terms, credentials, personal data, local paths, or screenshots with identifiers. Imported files require documented provenance and compatible licensing.
